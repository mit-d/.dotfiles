# Personal abbreviations - added by user
# This file is sourced after the built-in abbreviations
# Use `abbr -a` to add new ones (they'll be appended here)

# Command-position abbreviations (@key format)
abbrevs[@feat]="git switch -C feat/dmitten/WARH-__CURSOR__"
abbrevs[@bugs]="git switch -C bugs/dmitten/WARH-__CURSOR__"
abbrevs[@misc]="git switch -C misc/dmitten/__CURSOR__"
abbrevs[@t]="task __CURSOR__"
abbrevs[@manage]="docker exec -it jaguar-debug python manage.py __CURSOR__"
abbrevs[@manageh]="docker exec jaguar-debug python manage.py __CURSOR__ --help 2>/dev/null | bat -pl help"
abbrevs[@managesh]="docker exec -it jaguar-debugshell python manage.py __CURSOR__"
abbrevs[@db]="echo \"export DB_NAME=__CURSOR__\" >~/.db-env && source ~/.db-env"
abbrevs[@t]="task __CURSOR__"
abbrevs[@gd]="nvim -c ':DiffviewOpen'"
abbrevs[@gdm]="nvim -c ':DiffviewOpen origin/main...HEAD'"
abbrevs[@clod]="claude"
abbrevs[@c]="claude"
abbrevs[@dot]="cd ~/.dotfiles"
